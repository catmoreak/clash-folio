---

import Layout from '../layouts/Layout.astro';

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---


<div class="golden-ticker-bar">
  <div class="golden-ticker-fill"></div>
</div>
<Layout>
  <main class="clash-home-main">

    <section class="clash-home-card">
    <div class="clash-map-island island1"></div>
    <div class="clash-map-island island2"></div>
    <div class="clash-map-island island3"></div>
    <span class="pirate-anim pirate1">🏴‍☠️</span>
    <span class="pirate-anim pirate2">🏴‍☠️</span>
    <span class="pirate-anim pirate3">🏴‍☠️</span>
<style>
.pirate-anim {
  position: absolute;
  z-index: 3;

  background: linear-gradient(90deg, #fff 60%, #e2b007 100%);
  border-radius: 3px;
  box-shadow: 0 0 12px 4px #ffe066, 0 0 0 2px #fff;
  opacity: 0;
  transform: translateX(0);
  transition: opacity 0.1s, transform 0.5s cubic-bezier(0.68,-0.55,0.27,1.55);
}
.css-human-pirate.shoot .pirate-human-bullet {
  opacity: 1;
  transform: translateX(120px);
  transition: opacity 0.1s, transform 0.5s cubic-bezier(0.68,-0.55,0.27,1.55);
}
.pirate-human-body-main {
  position: absolute;
  left: 32px;
  top: 70px;
  width: 46px;
  height: 44px;
  background: #2e3a2e;
  border-radius: 22px 22px 28px 28px/22px 22px 36px 36px;
  border: 3px solid #145a1a;
  z-index: 1;
  outline: 2px solid #fff;
}
.pirate-human-leg {
  position: absolute;
  width: 12px;
  height: 32px;
  background: #222;
  border-radius: 8px;
  z-index: 0;
  outline: 2px solid #fff;
}
.pirate-human-leg-left {
  left: 38px;
  top: 110px;
}
.pirate-human-leg-right {
  left: 60px;
  top: 110px;
}
@keyframes shooter-fadein {
  0% { opacity: 0; transform: scale(0.7) translateY(60px); }
  100% { opacity: 1; transform: scale(1) translateY(0); }
}

.golden-ticker-bar {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 8px;
  background: linear-gradient(90deg, #bfa76f 0%, #e2b007 100%);
  z-index: 10001;
  overflow: hidden;
  box-shadow: 0 2px 8px #bfa76f88;
}
.golden-ticker-fill {
  position: absolute;
  left: -30vw;
  top: 0;
  height: 100%;
  width: 30vw;
  background: linear-gradient(90deg, #ffe066 0%, #e2b007 80%, #bfa76f 100%);
  opacity: 0.95;
  border-radius: 0 8px 8px 0;
  box-shadow: 0 0 16px 4px #e2b00788;
  animation: ticker-move 2.8s linear infinite;
}
@keyframes ticker-move {
  0% { left: -30vw; }
  100% { left: 100vw; }
}
</style>

    <section class="clash-home-card" style="z-index:4; background:rgba(255,242,34,0.18); box-shadow:0 8px 32px #bfa76f,0 0 32px 8px #e2b00755 inset; border:4px solid #bfa76f; border-radius:1.5rem;">
      <img src="/assets/clash.gif" alt="Clash of Clans Animation" class="clash-home-gif" />
      <div class="clash-home-shield">
        <span class="clash-sparkle sparkle1"></span>
        <span class="clash-sparkle sparkle2"></span>
      </div>
      <h1 class="clash-home-title">I am <span class="clash-coc">Shreyas</span> <span class="clash-emoji">🏴‍☠️</span></h1>
      <h2 class="clash-home-welcome">Welcome to my pirate island!<br>I love <span class="clash-coc">Science</span>, <span class="clash-coc">Clash of Clans</span> &amp; treasure hunts!</h2>
      <p class="clash-home-desc"><span class="clash-emoji">🍫</span> Chocolates, aye!<br><span class="clash-emoji">�️</span> Sometimes I get lost solving <span class="clash-coc">Maths</span> riddles.<br><span class="clash-emoji">🏝️</span> Explore the islands and beware of pirates!</p>
      <div class="clash-home-barbarian"></div>
    </section>
    </section>
  </main>
</Layout>

<style>
.clash-home-gif {
  display: block;
  margin: 0 auto 1rem auto;
  width: 120px;
  height: auto;
  border-radius: 1rem;
  box-shadow: 0 4px 16px #0007;
  animation: gif-pop 1.1s cubic-bezier(0.68,-0.55,0.27,1.55);
}
@keyframes gif-pop {
  0% { transform: scale(0.7) translateY(-30px); opacity: 0; }
  80% { transform: scale(1.05) translateY(10px); opacity: 1; }
  100% { transform: scale(1) translateY(0); opacity: 1; }
}
.clash-home-main {
  min-height: 70vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #8a6a3a;
  background-image:
    repeating-linear-gradient(135deg, #5a432222 0 2px, transparent 2px 40px),
    repeating-linear-gradient(-135deg, #5a432222 0 2px, transparent 2px 40px),
    radial-gradient(ellipse at 60% 20%, #a88b5a 60%, transparent 100%),
    radial-gradient(ellipse at 20% 80%, #a88b5a 50%, transparent 100%),
    url('data:image/svg+xml;utf8,<svg width="100%25" height="100%25" xmlns="http://www.w3.org/2000/svg"><path d="M0,30 Q40,0 80,30 T160,30 T240,30 T320,30 T400,30 T480,30 T560,30 T640,30 T720,30 T800,30 V570 Q760,600 720,570 T640,570 T560,570 T480,570 T400,570 T320,570 T240,570 T160,570 T80,570 T0,570 Z" fill="none" stroke="%23543a1a" stroke-width="18"/></svg>');
  padding: 2rem 0 3rem 0;
  position: relative;
  overflow: hidden;
  box-shadow: 0 0 120px 30px #3b2a13 inset, 0 0 0 18px #5a4322 inset;
  border-radius: 60px 48px 56px 52px/52px 60px 48px 56px;
  border: 10px solid #e2b007;
  filter: drop-shadow(0 0 24px #bfa76f88);
}
.clash-home-main::after {
  content: '';
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: 2;
  background:
    radial-gradient(circle at 20% 30%, #4b2e0a55 0 18%, transparent 60%),
    radial-gradient(circle at 80% 70%, #4b2e0a44 0 14%, transparent 60%),
    radial-gradient(circle at 60% 60%, #3b2a1355 0 10%, transparent 60%),
    radial-gradient(circle at 40% 80%, #2d1a0a33 0 8%, transparent 60%),
    radial-gradient(circle at 70% 20%, #3b2a1344 0 12%, transparent 60%);
  opacity: 0.55;
  mix-blend-mode: multiply;
  filter: blur(1.5px);
}
.clash-home-main::before {
  content: '';
  position: absolute;
  inset: 0;
  background: repeating-linear-gradient(135deg, #fff1 0 2px, transparent 2px 40px), repeating-linear-gradient(-135deg, #fff1 0 2px, transparent 2px 40px);
  opacity: 0.18;
  z-index: 0;
  pointer-events: none;
  animation: bg-move 8s linear infinite;
}
@keyframes bg-move {
  0% { background-position: 0 0, 0 0; }
  100% { background-position: 80px 80px, -80px -80px; }
}
.clash-home-card {
  background: rgba(255, 242, 34, 0.10);
  border: 4px solid #e2b007;
  border-radius: 2rem;
  box-shadow: 0 8px 32px #0008, 0 0 32px 8px #e2b00755 inset;
  padding: 2.5rem 2rem 2rem 2rem;
  text-align: center;
  max-width: 440px;
  font-family: 'Supercell-Magic', 'Comic Sans MS', cursive, sans-serif;
  color: #fff;
  position: relative;
  animation: home-card-in 1.1s cubic-bezier(0.68,-0.55,0.27,1.55);
  border-image: linear-gradient(120deg, #e2b007 60%, #fff7 100%) 1;
  overflow: visible;
}
@keyframes home-card-in {
  0% { transform: scale(0.7) translateY(60px); opacity: 0; }
  80% { transform: scale(1.05) translateY(-10px); opacity: 1; }
  100% { transform: scale(1) translateY(0); opacity: 1; }
}
.clash-home-shield {
  margin: 1rem auto 1rem auto;
  width: 120px;
  height: 90px;
  background: linear-gradient(120deg, #e2b007 60%, #bfa100 100%);
  border-radius: 50% 50% 60% 60%/60% 60% 100% 100%;
  border: 4px solid #fff7;
  box-shadow: 0 2px 12px #0007, 0 0 0 6px #e2b00744, 0 0 32px 8px #fff7 inset;
  position: relative;
  animation: shield-pop 1.2s cubic-bezier(0.68,-0.55,0.27,1.55);
  overflow: visible;
}
.clash-sparkle {
  position: absolute;
  width: 18px;
  height: 18px;
  background: radial-gradient(circle, #fff 60%, #fff7 100%);
  border-radius: 50%;
  opacity: 0.7;
  pointer-events: none;
  animation: sparkle-fade 1.6s infinite alternate;
  z-index: 2;
}
.sparkle1 {
  left: 18px;
  top: 10px;
  animation-delay: 0.2s;
}
.sparkle2 {
  right: 18px;
  top: 28px;
  animation-delay: 1s;
}
@keyframes sparkle-fade {
  0% { opacity: 0.2; transform: scale(0.7); }
  50% { opacity: 1; transform: scale(1.2); }
  100% { opacity: 0.2; transform: scale(0.7); }
}
@keyframes shield-pop {
  0% { transform: scale(0.5) rotate(-30deg); opacity: 0; }
  80% { transform: scale(1.1) rotate(10deg); opacity: 1; }
  100% { transform: scale(1) rotate(0); opacity: 1; }
}
.clash-home-title {
  font-size: 2.2rem;
  color: #ffe066;
  margin: 0.5rem 0 0.2rem 0;
  font-weight: 900;
  text-shadow:
    0 0 2px #000,
    0 0 4px #000,
    2px 2px 0 #000,
    0 0 16px #ffe066,
    0 0 32px #fff7,
    0 0 48px #ffe066;
  letter-spacing: 2px;
  animation: home-glow 2s infinite alternate;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
}
.clash-emoji {
  font-size: 1.3em;
  filter: drop-shadow(0 2px 4px #0008);
  vertical-align: middle;
}
@keyframes home-glow {
  from { text-shadow: 2px 2px 0 #000, 0 0 8px #e2b007, 0 0 16px #fff7; }
  to { text-shadow: 2px 2px 0 #000, 0 0 24px #e2b007, 0 0 32px #fff; }
}
.clash-home-welcome {
  font-size: 1.3rem;
  color: #fff;
  margin: 0 0 1.2rem 0;
  text-shadow: 1px 1px 0 #7c4a03;
  animation: home-fadein 1.5s 0.3s both;
}
.clash-home-desc {
  font-size: 1.1rem;
  color: #fff;
  margin-bottom: 1.5rem;
  text-shadow: 1px 1px 0 #000;
  animation: home-fadein 1.5s 0.5s both;
}
@keyframes home-fadein {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}
.clash-coc {
  color: #ffe066;
  font-weight: 900;
  text-shadow:
    0 0 2px #000,
    0 0 4px #000,
    0 0 12px #ffe066,
    0 0 24px #fff7;
}
.clash-home-barbarian {
  height: 90px;
  width: 70px;
  margin: 1.5rem auto 0 auto;
  background: linear-gradient(120deg, #f7c873 60%, #b97a2a 100%);
  border-radius: 30% 30% 50% 50%/40% 40% 100% 100%;
  border: 3px solid #fff7;
  box-shadow: 0 2px 8px #0006, 0 0 0 2px #fff4 inset, 0 0 24px 6px #0008;
  position: relative;
  animation: barbarian-bounce 1.2s 0.7s both cubic-bezier(0.68,-0.55,0.27,1.55);
  z-index: 1;
}
@keyframes barbarian-bounce {
  0% { transform: scale(0.5) translateY(60px); opacity: 0; }
  80% { transform: scale(1.1) translateY(-10px); opacity: 1; }
  100% { transform: scale(1) translateY(0); opacity: 1; }
}
